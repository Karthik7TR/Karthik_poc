# Application properties common to all environments

# Hibernate
hibernate.show.sql=false
hibernate.cache.provider.class=org.hibernate.cache.HashtableCacheProvider
hibernate.dialect=org.hibernate.dialect.Oracle10gDialect

# Directory that holds the Spring Batch job definition spring bean XML files.
# All *.xml files in this directory will be read into the Spring application context.
job.directory=/apps/Codes/MP2/jobs
