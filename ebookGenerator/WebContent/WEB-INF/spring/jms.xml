<!-- 
	JMS configuration for connection factories, destinations (queues), queue listeners, and message-driven POJO's (MDP's).
	Application/web server independent configuration of Spring JMS.
	Assumes the following:
	1) The JMS connection factory administered object(s) is defined in the web server naming service (JNDI).
	2) The JMS queue destinations are also defined in the web server naming service (JNDI).
-->

<beans xmlns="http://www.springframework.org/schema/beans"
  	   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  	   xmlns:context="http://www.springframework.org/schema/context"
  	   xmlns:p="http://www.springframework.org/schema/p"
  	   xmlns:jee="http://www.springframework.org/schema/jee"
  	   xmlns:jms="http://www.springframework.org/schema/jms"
  	   xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
  	   					   http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.0.xsd
 	   					   http://www.springframework.org/schema/jee http://www.springframework.org/schema/jee/spring-jee-3.0.xsd
 	   					   http://www.springframework.org/schema/jms http://www.springframework.org/schema/jms/spring-jms-3.0.xsd">

	<!-- JNDI lookup of the JMS Connection Factory objects -->
    <jee:jndi-lookup id="queueConnectionFactory" jndi-name="jms/QueueConnectionFactory" expected-type="javax.jms.ConnectionFactory" resource-ref="true"/>
	
	<!-- These queue(s) have JMS listeners attached to them, see listener definition below -->
    <jee:jndi-lookup id="highPriorityJobQueue" jndi-name="jms/HighPriorityJobQueue" expected-type="javax.jms.Queue" resource-ref="true"/>
    <jee:jndi-lookup id="normalPriorityJobQueue" jndi-name="jms/NormalPriorityJobQueue" expected-type="javax.jms.Queue" resource-ref="true"/>
	
	<bean id="jmsTemplate" class="org.springframework.jms.core.JmsTemplate"
		  p:connectionFactory-ref="queueConnectionFactory"
		  p:receiveTimeout="250"/>

	<!-- Message-driven POJO handler beans - used by the listener-containers defined below. 
		 These implement the onMessage() method. -->
<!--

	<bean id="jobStartImmediateMdp" class="com.thomsonreuters.uscl.ereader.orchestrate.engine.mdp.ImmediateJobStartMdp"/>

   		Queue Listeners.
		 Create the message queue listeners and map the JMS provider queue's to their corresponding message-driven POJO (MDP).
		 The listener is the intermediary between the messaging provider as represented by the connection factory object, and the MDP.

	<bean id="jndiDestinationResolver" class="org.springframework.jms.support.destination.JndiDestinationResolver"/>
	<jms:listener-container connection-factory="queueConnectionFactory"
							destination-type="queue"
							destination-resolver="jndiDestinationResolver">
		<jms:listener id="jobControlRequestListener"
					  destination="java:comp/env/jms/StartJobImmediateQueue" ref="jobStartImmediateMdp"/>
	</jms:listener-container>
  -->
	
</beans>
