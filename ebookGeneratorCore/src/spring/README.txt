The spring bean files in this directory are shared between the
ebookGenerator and ebookManager web projects.

They are loaded via contextConfigLocation path:
"classpath:spring/*.xml" in WEB-INF/web.xml for both apps.