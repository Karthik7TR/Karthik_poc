<?xml version="1.0"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	   xmlns:security="http://www.springframework.org/schema/security"
	   xmlns:p="http://www.springframework.org/schema/p"
	   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	   xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd 
	   					   http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-3.0.xsd">

	<!-- This represents what the CAS folks call the "CAS service" -
		 the client web app using the SSO CAS server web app.-->
	<bean id="serviceProperties" class="org.springframework.security.cas.ServiceProperties"
		  p:service="${cas.client.web.url}/j_spring_cas_security_check"
     	  p:sendRenew="false"/>
  	 
	<bean id="casAuthenticationProvider" class="org.springframework.security.cas.authentication.CasAuthenticationProvider"
	      p:authenticationUserDetailsService-ref="userDetailsService"
	      p:serviceProperties-ref="serviceProperties"
	      p:key="tr_cas">
	    <property name="ticketValidator">
	     	<bean class="org.jasig.cas.client.validation.Cas20ServiceTicketValidator">
	        	<constructor-arg index="0" value="${cas.server.web.url}"/>
	        </bean>
	    </property>
  	</bean>
  	
	<bean id="casAuthenticationEntryPoint" class="org.springframework.security.cas.web.CasAuthenticationEntryPoint"
  		  p:loginUrl="${cas.server.web.url}/login"
  		  p:serviceProperties-ref="serviceProperties"/>
  	
	<bean id="casAuthenticationFilter" class="org.springframework.security.cas.web.CasAuthenticationFilter"
  		  p:authenticationManager-ref="authenticationManager"/>

  	<security:authentication-manager alias="authenticationManager">
    	<security:authentication-provider ref="casAuthenticationProvider"/>
  	</security:authentication-manager>

	<security:http entry-point-ref="casAuthenticationEntryPoint" access-denied-page="/accessDenied.mvc">
   		<security:custom-filter position="FORM_LOGIN_FILTER" ref="casAuthenticationFilter"/>
   		
   		<!-- Access Control - URL/role based -->
		<security:intercept-url pattern="/**" access="IS_AUTHENTICATED_FULLY"/>
		
		<!-- Logout -->
		<!-- 
		<security:logout logout-success-url="/afterLogout.mvc"/>
		 -->
	</security:http>

</beans>